spring:
  application:
    name: encurtador

  datasource:
    url: jdbc:mysql://localhost:3307/encurtador
    username: mysql
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
